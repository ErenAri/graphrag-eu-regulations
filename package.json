{
  "name": "eu-fintech-reg-assistant",
  "private": true,
  "scripts": {
    "typecheck": "npm run --workspace packages/core typecheck && npm run --workspace apps/web typecheck"
  },
  "workspaces": [
    "apps/web",
    "packages/core"
  ]
}
