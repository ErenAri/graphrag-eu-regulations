# Optional CUDA runtime overlay for local development on NVIDIA GPUs.
# Install after requirements-dev:
#   python -m pip install -r apps/api/requirements-gpu-cu126.txt
--index-url https://download.pytorch.org/whl/cu126
torch==2.10.0+cu126
